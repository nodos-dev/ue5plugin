[FilterPlugin]
; BuildPlugin packages Source, Resources, Content, Shaders and the binaries, and
; nothing else, so the settings default has to be named here to travel with the
; plugin. The installer build renames it to EditorSettings.ini.
/Config/EditorSettings.ini.template
